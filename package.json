{
  "name": "tim-ai-satset",
  "version": "1.0.0",
  "description": "Aplikasi Lisensi AI Satset",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r functions dist/functions"
  },
  "dependencies": {
    "firebase": "^10.14.1"
  }
}
